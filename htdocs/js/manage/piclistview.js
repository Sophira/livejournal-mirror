PictureListView = new Class(ManageListView, {

  init: function () {
    PictureListView.superClass.init.apply(this, arguments);
  },

  addHeaders: function () {

  },

  render: function (data) {
  },

  drawRow: function (table, gallery) {
    var view = this.view;
  }

});
