<?_code # -*-bml-*-
{
    use strict;

    my $err = sub {
        my $msg = shift;
        return JSON::objToJson({
            'alert' => $msg,
        });
    };

    # get user
    my $u = FB::get_remote()
        or return $err->("User not logged in.");

    my $ret = {diskfree_widget => $u->diskfree_widget};

    sleep 1 if $FB::IS_DEV_SERVER;

    return JSON::objToJson($ret);
}
_code?>
